[tool.poetry]
name = "tsu-nlp"
version = "0.1.0"
description = "Text normalization service for Russian language"
authors = ["Your Name <your.email@example.com>"]

[tool.poetry.dependencies]
python = ">=3.9,<3.13"
torch = "^2.1.0"
transformers = "^4.36.0"
pandas = "^2.1.0"
tqdm = "^4.66.0"
onnxruntime-gpu = "^1.16.0"
numpy = "^1.24.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"